# SurveyHub - Платформа для создания и проведения онлайн-опросов

Полнофункциональная система для создания, публикации и анализа онлайн-опросов с современным веб-интерфейсом и аналитикой.

## 🚀 Технологии

### Backend
- **FastAPI** - современный веб-фреймворк для Python
- **PostgreSQL** - реляционная база данных
- **SQLAlchemy** (async) - ORM для работы с БД
- **Alembic** - миграции базы данных
- **Pydantic** - валидация данных
- **JWT** (python-jose) - аутентификация с access и refresh токенами
- **bcrypt** - хеширование паролей
- **Uvicorn** - ASGI сервер

### Frontend
- **Nuxt 4** - фреймворк на основе Vue.js
- **Vue 3** - прогрессивный JavaScript фреймворк
- **TypeScript** - типизированный JavaScript
- **TailwindCSS** - utility-first CSS фреймворк
- **Pinia** - state management для Vue
- **Chart.js / vue-chartjs** - визуализация данных
- **Vite** - быстрый сборщик

### DevOps
- **Docker** - контейнеризация приложений
- **Docker Compose** - оркестрация контейнеров

## ✨ Реализованный функционал

### Аутентификация и авторизация
- ✅ Регистрация и вход пользователей
- ✅ JWT аутентификация с access и refresh токенами
- ✅ Автоматическое обновление токенов при истечении
- ✅ Защита маршрутов для авторизованных пользователей
- ✅ Роли пользователей (user, admin)

### Управление опросами
- ✅ Создание опросов с заголовком и описанием
- ✅ Редактирование опросов
- ✅ Публикация и снятие с публикации опросов
- ✅ Просмотр списка своих опросов в дашборде
- ✅ Просмотр публичных опросов для всех пользователей
- ✅ Удаление опросов

### Типы вопросов
- ✅ **Одиночный выбор** (single choice) - радио-кнопки
- ✅ **Множественный выбор** (multi choice) - чекбоксы
- ✅ **Шкала оценок** (scale) - числовая шкала с настраиваемым диапазоном
- ✅ **Текстовый ответ** (text) - свободный текст

### Управление вопросами
- ✅ Добавление вопросов к опросу
- ✅ Редактирование вопросов
- ✅ Удаление вопросов
- ✅ Настройка обязательности вопросов
- ✅ Добавление вариантов ответов для single/multi вопросов
- ✅ Настройка порядка вопросов

### Прохождение опросов
- ✅ Просмотр опубликованных опросов
- ✅ Прохождение опросов (авторизованными и анонимными пользователями)
- ✅ Валидация обязательных вопросов
- ✅ Защита от повторного прохождения:
  - Для авторизованных пользователей - по `user_id`
  - Для анонимных пользователей - по `session_id` (cookie)
- ✅ Отображение статистики после прохождения

### Аналитика и результаты
- ✅ Общая статистика по опросу (количество ответов)
- ✅ Детальная аналитика по каждому вопросу:
  - Для single/multi: количество и процент выбора каждого варианта
  - Для scale: гистограмма распределения и среднее значение
  - Для text: таблица всех текстовых ответов с датами
- ✅ Визуализация данных с помощью графиков (Bar, Pie)
- ✅ Интерактивный выбор вопроса для просмотра аналитики

### UI/UX
- ✅ Современный адаптивный дизайн
- ✅ Градиенты и анимации
- ✅ Карточки и компоненты с hover-эффектами
- ✅ Адаптивная верстка для мобильных устройств
- ✅ Интуитивная навигация
- ✅ Обработка состояний загрузки
- ✅ Обработка ошибок с понятными сообщениями

## 🎯 Особенности

1. **Автоматическое обновление токенов** - при истечении access токена система автоматически обновляет его через refresh token
2. **Защита от повторных прохождений** - реализована для авторизованных и анонимных пользователей
3. **Гибкая система типов вопросов** - поддержка 4 различных типов вопросов с соответствующими вариантами ответов
4. **Мощная аналитика** - детальная статистика с визуализацией для каждого типа вопроса
5. **Анонимные ответы** - возможность проходить опросы без регистрации
6. **Автоматические миграции БД** - Alembic миграции применяются автоматически при запуске backend
7. **SSR-оптимизация** - правильная обработка гидратации в Nuxt для предотвращения ошибок
8. **TypeScript** - полная типизация на frontend для надежности кода
9. **Адаптивный дизайн** - корректное отображение на всех устройствах

## 📁 Структура проекта

```
survey-system-pm05/
├── backend/                 # FastAPI приложение
│   ├── app/
│   │   ├── api/            # API endpoints
│   │   │   └── v1/
│   │   │       ├── auth.py      # Аутентификация
│   │   │       ├── surveys.py   # Управление опросами
│   │   │       ├── responses.py # Ответы на опросы
│   │   │       └── analytics.py  # Аналитика
│   │   ├── core/           # Конфигурация
│   │   ├── services/       # Бизнес-логика
│   │   │   └── analytics.py
│   │   ├── crud.py         # CRUD операции
│   │   ├── models.py       # SQLAlchemy модели
│   │   ├── schemas.py      # Pydantic схемы
│   │   ├── auth.py         # JWT и пароли
│   │   ├── db.py           # Подключение к БД
│   │   └── main.py         # Точка входа
│   ├── alembic/            # Миграции БД
│   │   └── versions/
│   ├── Dockerfile
│   └── requirements.txt
│
├── frontend/                # Nuxt 4 приложение
│   ├── pages/              # Страницы
│   │   ├── index.vue            # Главная (публичные опросы)
│   │   ├── login.vue            # Вход
│   │   ├── register.vue         # Регистрация
│   │   ├── dashboard.vue        # Дашборд пользователя
│   │   ├── profile.vue          # Профиль
│   │   └── surveys/
│   │       ├── create.vue        # Создание опроса
│   │       ├── [id]/
│   │       │   ├── edit.vue      # Редактирование
│   │       │   ├── take.vue      # Прохождение
│   │       │   └── results.vue   # Результаты
│   ├── stores/             # Pinia stores
│   │   ├── auth.ts         # Состояние аутентификации
│   │   └── surveys.ts      # Состояние опросов
│   ├── composables/        # Композаблы
│   │   └── useApi.ts       # API клиент с автообновлением токенов
│   ├── plugins/            # Nuxt плагины
│   ├── assets/             # Статические файлы
│   │   └── css/
│   │       └── tailwind.css
│   ├── Dockerfile
│   ├── nuxt.config.ts
│   ├── tailwind.config.cjs
│   └── package.json
│
├── db_data/                # Данные PostgreSQL (volume)
├── docker-compose.yml       # Конфигурация Docker Compose
├── .env.example            # Пример переменных окружения
└── README.md
```

## 🚀 Запуск проекта

### Предварительные требования

- Docker и Docker Compose установлены
- Git для клонирования репозитория

### Быстрый старт с Docker Compose

1. **Клонируйте репозиторий:**

2. **Создайте файл `.env` в корне проекта, в папке frontend и backend:**
```bash
cp .env.example .env
```

Отредактируйте `.env` при необходимости (по умолчанию значения подходят для тестирования).

3. **Запустите проект:**
```bash
docker-compose up --build
```

4. **Откройте в браузере:**
- Frontend: http://localhost:3000
- Backend API: http://localhost:8000
- API документация: http://localhost:8000/docs

### Локальный запуск (без Docker)

#### Backend

1. **Установите зависимости:**
```bash
cd backend
pip install -r requirements.txt
```

2. **Настройте переменные окружения:**
Создайте `.env` файл в папке `backend/`:
```env
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/surveys_db
SECRET_KEY=your-secret-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7
```

3. **Запустите PostgreSQL** (если еще не запущен)

4. **Примените миграции:**
```bash
alembic upgrade head
```

5. **Запустите сервер:**
```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

#### Frontend

1. **Установите зависимости:**
```bash
cd frontend
npm install
```

2. **Настройте переменные окружения:**
Создайте `.env` файл в папке `frontend/`:
```env
NUXT_PUBLIC_API_BASE=http://localhost:8000
```

3. **Запустите dev-сервер:**
```bash
npm run dev
```

Frontend будет доступен по адресу http://localhost:3000

## 📚 API Endpoints

### Аутентификация

- `POST /api/v1/auth/register` - Регистрация пользователя
- `POST /api/v1/auth/login` - Вход (возвращает access и refresh токены)
- `POST /api/v1/auth/refresh` - Обновление токенов
- `GET /api/v1/auth/me` - Получение информации о текущем пользователе

### Опросы

- `GET /api/v1/surveys` - Список опросов (с фильтрами `owner_id`, `published`)
- `POST /api/v1/surveys` - Создание опроса (требует авторизации)
- `GET /api/v1/surveys/{id}` - Детали опроса
- `PUT /api/v1/surveys/{id}` - Обновление опроса (требует авторизации)
- `DELETE /api/v1/surveys/{id}` - Удаление опроса (требует авторизации)
- `POST /api/v1/surveys/{id}/publish` - Публикация/снятие с публикации

### Вопросы

- `POST /api/v1/surveys/{id}/questions` - Добавление вопроса
- `PUT /api/v1/surveys/questions/{question_id}` - Обновление вопроса
- `DELETE /api/v1/surveys/questions/{question_id}` - Удаление вопроса
- `POST /api/v1/surveys/questions/{question_id}/options` - Добавление варианта ответа

### Ответы

- `POST /api/v1/surveys/{id}/responses` - Отправка ответов на опрос
- `GET /api/v1/surveys/{id}/responses/check` - Проверка, проходил ли пользователь опрос
- `GET /api/v1/surveys/{id}/responses` - Список ответов (для владельца опроса)
- `GET /api/v1/responses/{response_id}` - Детали ответа

### Аналитика

- `GET /api/v1/surveys/{id}/analytics` - Полная аналитика по опросу
- `GET /api/v1/surveys/{id}/questions/{question_id}/analytics` - Аналитика по вопросу


## 🐳 Docker команды

```bash
# Запуск в фоновом режиме
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down

# Пересборка образов
docker-compose up --build

# Остановка с удалением volumes (очистка БД)
docker-compose down -v
```

## 📊 База данных

Проект использует PostgreSQL с автоматическими миграциями через Alembic. При первом запуске все таблицы создаются автоматически.

Основные таблицы:
- `users` - пользователи системы
- `surveys` - опросы
- `questions` - вопросы
- `options` - варианты ответов
- `responses` - ответы на опросы
- `answer_values` - значения ответов
- `answer_options` - связь ответов с вариантами

## 🔒 Безопасность

- Хеширование паролей с помощью bcrypt
- JWT токены для аутентификации
- Refresh токены для безопасного обновления сессий
- Защита от повторных прохождений опросов
- Валидация данных на уровне API (Pydantic)
- CORS настройки для безопасности